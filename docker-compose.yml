services:
  db:
    image: mysql:8
    environment:
      MYSQL_DATABASE: pluribus
      MYSQL_USER: pluribus
      MYSQL_PASSWORD: pluribus_secret
      MYSQL_ROOT_PASSWORD: pluribus_root_secret
    ports:
      - "9121:3306"
    restart: unless-stopped
    volumes:
      # Named volume: data survives container rebuilds and `docker compose down`.
      # Wiped only if you remove this volume, e.g. `docker compose down -v` or
      # `docker volume rm pluribus_db_data`.
      - db_data:/var/lib/mysql
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "mysqladmin ping -h localhost -upluribus -ppluribus_secret || exit 1",
        ]
      interval: 3s
      timeout: 5s
      retries: 30
      start_period: 15s

  phpmyadmin:
    image: mirror.gcr.io/phpmyadmin/phpmyadmin:latest
    ports:
      - "9124:80"
    environment:
      PMA_HOST: db
      PMA_USER: pluribus
      PMA_PASSWORD: pluribus_secret
      UPLOAD_LIMIT: 64M
    depends_on:
      db:
        condition: service_healthy

  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "9122:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./backend:/var/www/html
    depends_on:
      db:
        condition: service_healthy

  # Frontend uses a named volume for node_modules so the bind mount of ./frontend
  # does not hide host node_modules (often wrong OS/arch). After adding new npm
  # packages, `npm install` on each start syncs package.json + lockfile into the
  # volume. If deps still look wrong, reset the volume:
  #   docker compose down
  #   docker volume rm pluribus_frontend_node_modules   # see: docker volume ls | grep frontend
  #   docker compose up -d frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "9123:9123"
    environment:
      VITE_API_BASE_URL: http://localhost:9122
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
    command: >
      sh -ec "npm install --no-audit --no-fund && exec npm run dev -- --host 0.0.0.0 --port 9123"
    depends_on:
      - api

volumes:
  db_data:
    name: pluribus_db_data
  frontend_node_modules:
